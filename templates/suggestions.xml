<?xml version="1.0" encoding="UTF-8"?>
<toplevel>
    <CompleteSuggestion query="{{.Query | escapeAttr}}">
    {{range .Suggestions}}
        <suggestion data="{{. | escapeAttr}}"/>
    {{end}}
    </CompleteSuggestion>
</toplevel>